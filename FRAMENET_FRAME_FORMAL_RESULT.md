# FrameNet frame-disambiguation formal confirmation result

Date: 2026-08-12. Status: **structurally invalid before method evaluation**.

## Outcome

The public screen was a strong intervention GO and the complete pre-private
method lock passed. The lock covered 14 method files, a deterministic 12-row
synthetic dry run, 6,475 records, 433 units, the `risk_per_second` route, 750
uniform sentinels, 100 repetitions, and the unchanged operating gates. All 32
project tests passed immediately before the private unlock.

The fixed NLTK FrameNet 1.7 archive was then opened exactly once. The formal
alignment scanned 13,679 lexicographic and full-text XML members and compared
only the frozen exact sentence/target/span keys. Results were:

- 433 expected public keys;
- 431 uniquely aligned keys and units;
- 2 missing exact keys;
- 0 ambiguous keys;
- 0 truth ambiguities among aligned units;
- 0 candidate-coverage failures among aligned records.

Because two public keys had no exact match, complete independent truth coverage
failed. The protocol prohibits dropping those units, fuzzy text matching,
offset repair, alternate-source substitution, or changing the truth key after
opening. The confirmation therefore stopped before constructing the error
array and before any score-time or risk-per-second episode.

## Interpretation

This is not `v2 external efficiency NO-GO`: the method was never evaluated.
It is a candidate-level structural invalidation. The public findings remain
valid outcome-blind evidence: cost CV was 1.591, minimum public opportunity was
91.63%, every retained unit was repeated, and the frozen route was
`risk_per_second`.

The dataset may now be used only for explicitly exploratory truth-mapping or
mechanism analysis. It cannot be repaired and reused as a confirmatory v2
dataset. The next formal attempt requires a different untouched dataset whose
independent truth covers every exact task key.

Artifacts:

- `V2_FRAMENET_FRAME_PUBLIC_PROTOCOL.md`;
- `FRAMENET_FRAME_PUBLIC_RESULT.md`;
- `V2_FRAMENET_FRAME_FORMAL_PROTOCOL.md`;
- `outputs/framenet_frame_formal_confirmation/design_pre_private.json`;
- `outputs/framenet_frame_formal_confirmation/method_lock_manifest.json`;
- `outputs/framenet_frame_formal_confirmation/structural_invalidation.json`.
